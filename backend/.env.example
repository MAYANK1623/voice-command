PORT=4000
NODE_ENV=development
# Comma-separated list of allowed origins for CORS in production.
# In development, localhost:5173 (the Vite dev server) is always allowed.
CORS_ORIGIN=http://localhost:5173
